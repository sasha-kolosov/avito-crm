class Database {
    constructor(database) {
        this.database = database
    }

    get() {
        try {
            
        } catch (error) {
            return `Server answer ${error}`
        }
    }

    post() {
        try {

        } catch (error) {
            return `Server answer ${error}`
        }
    }

    update() {
        try {

        } catch (error) {
            return `Server answer ${error}`
        }
    }

    delete() {
        try {

        } catch (error) {
            return `Server answer ${error}`
        }
    }

}